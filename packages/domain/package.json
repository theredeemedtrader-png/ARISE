{
  "name": "@arise/domain",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "lint": "echo domain:lint"
  },
  "devDependencies": {
    "vitest": "^3.2.4"
  }
}
